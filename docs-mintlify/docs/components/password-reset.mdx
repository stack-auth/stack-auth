---
title: "<PasswordReset />"
description: Renders a password reset component based on the provided search parameters and optional full page display.
---

Renders a password reset component based on the provided search parameters and optional full page display.

## Props

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `searchParams` | `Record<string, string>` | *required* | An object containing search parameters, including the password reset code. |
| `fullPage` | `boolean` | `false` | Determines whether to display the component in full page mode. |

## Example

```tsx title="app/reset-password.tsx"
import { PasswordReset } from '@stackframe/stack';

export function ResetPasswordPage(props: { searchParams: Record<string, string> }) {
  return (
    <PasswordReset
      searchParams={props.searchParams}
      fullPage={true}
    />
  );
}
```
