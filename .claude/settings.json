{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(pnpm typecheck:*)",
      "Bash(pnpm test:*)",
      "Bash(pnpm build:*)",
      "Bash(pnpm lint:*)",
      "Bash(find:*)",
      "Bash(ls:*)",
      "Bash(pnpm codegen)",
      "Bash(pnpm vitest run:*)",
      "Bash(pnpm eslint:*)"
    ],
    "deny": []
  },
  "includeCoAuthoredBy": false,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.js|*.jsx|*.ts|*.tsx) d=$(dirname \"$f\"); while [ \"$d\" != \"/\" ] && [ ! -f \"$d/package.json\" ]; do d=$(dirname \"$d\"); done; (cd \"$d\" && pnpm exec eslint --fix \"$f\") >/dev/null 2>&1 || true;; esac; }"
          }
        ]
      }
    ]
  }
}
