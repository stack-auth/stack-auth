replicaCount: 1

image:
  namespace: gen-ai-template-stag
  pullPolicy: IfNotPresent
  pullSecret: gcr-secret
  repositoryUrl: asia-southeast2-docker.pkg.dev/${PROJECT_ID}/projects/stack-auth/server
  tag: "${BUILD_NUMBER}"
  # repositoryUrl: stackauth/server
  # tag: 2bcbe01
  ports:
    - name: dashboard
      port: 8101
      targetPort: 8101
      nodePort: null
      Values:
        service:
          type: ClusterIP
    - name: api
      port: 8102
      targetPort: 8102
      nodePort: null
      Values:
        service:
          type: ClusterIP

strategy:
  type: RollingUpdate
  rollingUpdate:
    # The default function is used to provide a fallback value if the specified value is not defined or is empty. In Helm, 0 (zero) is treated as an empty value (falsy)
    maxSurge: "0"
    maxUnavailable: 1

configmap:
  create: true
  configmapRef:
    name: stackauth-configmap
secret:
  create: true
  secretRef:
    name: stackauth-secret

resources:
  limits:
    memory: 2Gi
  requests:
    cpu: 512m
    memory: 768Mi

pvc:
  create: false


ingress:
  name: add-ons-stackauth
  className: "alb"
  groupname: alb-gdplabs-shared-kubernetes
  healthcheckpath: "/"
  serviceannotations:
    alb.ingress.kubernetes.io/tags: "Environment=stag,Project=gen-ai,Organization=gdplabs,Owner=glx-sre@gdplabs.id,CreatedOn=17/06/2025"
  rules:
    - host: stag-stackauth-gdplabs-gen-ai-starter.obrol.id
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ssl-redirect
                port:
                  name: use-annotation
          - path: /
            pathType: Prefix
            backend:
              service:
                name: add-ons-stackauth-service
                port:
                  number: 8101
    - host: stag-api-stackauth-gdplabs-gen-ai-starter.obrol.id
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ssl-redirect
                port:
                  name: use-annotation
          - path: /
            pathType: Prefix
            backend:
              service:
                name: add-ons-stackauth-service
                port:
                  number: 8102
