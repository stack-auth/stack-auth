// Will be implemented later when we have the correct import paths
export const metadataImage = {
  withImage: (slugs: string[], metadata: { title: string; description: string }) => ({
    title: metadata.title,
    description: metadata.description
  })
};
