import { createMDXSource } from 'fumadocs-mdx';

// Create a simple source with empty docs array for now
// The actual content will be loaded from the content/docs directory
export const source = createMDXSource([]);

// We'll update this implementation once we have the correct structure
// This is a placeholder to allow the app to compile
