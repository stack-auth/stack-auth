Please compare `origin/dev` to `origin/main` (feel free to check out the dev branch if needed) and ensure that all migrations are backwards compatible. In what ways (DB, API, or otherwise) could breakage occur? Report the result to me in detail. Anything else that's scary that could occur, or that we should think about while migrating? Should we migrate first and upgrade the code second, or the other way around? Are rollbacks safe? Think hard. Unless specified otherwise, assume the database will be deployed before the backend, and the backend will be deployed before the frontend. When writing the report, make sure to explain what change happened and/or caused this, as the person reading your report is probably not the same person who made the changes and isn't aware of all the context.
