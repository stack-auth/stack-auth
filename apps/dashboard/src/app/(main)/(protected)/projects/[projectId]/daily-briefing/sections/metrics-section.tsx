"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function MetricsSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="metrics" />;
}
