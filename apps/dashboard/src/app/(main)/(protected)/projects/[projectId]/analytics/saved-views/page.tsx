import { AppTodoPage } from "../../app-todo-page";

export const metadata = { title: "Saved Views" };

export default function Page() {
  return <AppTodoPage title="Saved Views" description="Return to reusable Analytics queries and table configurations." />;
}
