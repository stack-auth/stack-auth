"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function OneThingSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="one-thing" />;
}
