"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function RiskRadarSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="risk" />;
}
