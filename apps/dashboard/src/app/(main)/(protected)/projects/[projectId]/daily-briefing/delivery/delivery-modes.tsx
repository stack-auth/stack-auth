"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "../sections/section-stub";

export function DeliveryModesSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="delivery" />;
}
