"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function TeamPulseSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="team-pulse" />;
}
