"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function SalesStrategySection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="sales" />;
}
