import { AppTodoPage } from "../../app-todo-page";

export const metadata = { title: "Funnels" };

export default function Page() {
  return <AppTodoPage title="Funnels" description="Understand where users convert or drop out across ordered events." />;
}
