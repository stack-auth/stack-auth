"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function ReplaysSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="replays" />;
}
