"use client";

import { type SectionWidgetProps } from "../briefing-config";
import { SectionStub } from "./section-stub";

export function BenchmarksSection(_props: SectionWidgetProps) {
  return <SectionStub sectionId="benchmarks" />;
}
